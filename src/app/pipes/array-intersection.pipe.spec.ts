import { ArrayIntersectionPipe } from './array-intersection.pipe';

describe('ArrayIntersectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayIntersectionPipe();
    expect(pipe).toBeTruthy();
  });
});
